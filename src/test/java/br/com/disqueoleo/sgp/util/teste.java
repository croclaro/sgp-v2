package br.com.disqueoleo.sgp.util;

import java.util.List;

import br.com.disqueoleo.sgp.dao.FornecedorDAO;
import br.com.disqueoleo.sgp.domain.Fornecedor;

public class teste {
	/*
	 * @Test public void salvar(){ FuncionarioDAO funcionarioDAO = new
	 * FuncionarioDAO(); Funcionario funcionario = funcionarioDAO.buscar(1l);
	 * 
	 * Usuario usuario = new Usuario(); usuario.setStatus(true);
	 * usuario.setFuncionario(funcionario);
	 * usuario.setDataUsuario("12/-12/2017 06:00:00"); usuario.setSenha("123");
	 * 
	 * SimpleHash hash = new SimpleHash("md5", usuario.getSenha());
	 * usuario.setSenha(hash.toHex());
	 * 
	 * UsuarioDAO usuarioDAO = new UsuarioDAO(); usuarioDAO.salvar(usuario);
	 * 
	 * System.out.println("SALVO COM SUCESSO");
	 * 
	 * 
	 * }
	 */
	
	public static void main(String[] args) {
		
		Fornecedor fornecedor = null;
		List<Fornecedor> fornecedores = null;
		
		int i = 0;
		
		while(i<0) {
			FornecedorDAO fornecedorDAO = new FornecedorDAO();
			fornecedores.add(fornecedor);
			i++;
		}
	}

}
